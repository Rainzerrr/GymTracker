// Shared by the workout hero and the rest-day banner so the page below never
// shifts when the selected day switches between them.
export const HOME_HERO_HEIGHT_REM = 13.125

// Room reserved under the hero for the start / view button. The rest-day
// banner has no button, so it grows by this much to keep the layout still.
export const HOME_CTA_HEIGHT_REM = 3.25
