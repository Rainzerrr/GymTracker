import type { AppSettings } from '../types/app-settings'

export const DEFAULT_APP_SETTINGS: AppSettings = {
  theme: 'auto',
  restAlert: true,
  keepScreenAwake: true,
  defaultRest: '90s',
  postureRoutineEnabled: true,
}
